apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: oficina

resources:
  - ../../base

labels:
  - includeSelectors: true
    pairs:
      app.kubernetes.io/instance: producao

images:
  - name: oficina-api
    newName: PLACEHOLDER_ECR_REPO
    newTag: PLACEHOLDER_TAG
