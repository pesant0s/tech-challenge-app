apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# O Secret oficina-secret é criado pelo pipeline a partir do Secrets Manager.
resources:
  - deployment.yaml
  - service.yaml
  - hpa.yaml
  - configmap.yaml
  - pdb.yaml
